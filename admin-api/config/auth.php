<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 認証のデフォルト設定
    |--------------------------------------------------------------------------
    |
    | アプリケーションのデフォルトの認証「ガード」とパスワードリセットの
    | オプションを制御します。必要に応じて変更できますが、
    | ほとんどのアプリケーションにとって最適な初期設定です。
    |
    */

    'defaults' => [
        'guard' => 'web',
        'passwords' => 'users',
    ],

    /*
    |--------------------------------------------------------------------------
    | 認証ガード
    |--------------------------------------------------------------------------
    |
    | アプリケーションの認証ガードを定義します。
    | セッションストレージとEloquentユーザープロバイダーを使用する
    | デフォルト設定が用意されています。
    |
    | すべての認証ドライバーにはユーザープロバイダーがあります。
    | これはデータベースやその他のストレージからユーザーを
    | 取得する方法を定義します。
    |
    | サポート: "session"
    |
    */

    'guards' => [
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],

        'admin' => [
            'driver' => 'jwt',
            'provider' => 'admins',
        ],

        'api' => [
            'driver' => 'jwt',
            'provider' => 'users',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | ユーザープロバイダー
    |--------------------------------------------------------------------------
    |
    | すべての認証ドライバーにはユーザープロバイダーがあります。
    | これはデータベースやその他のストレージからユーザーを
    | 取得する方法を定義します。
    |
    | 複数のユーザーテーブルやモデルがある場合、各モデル/テーブルを
    | 表す複数のソースを設定できます。これらのソースは、
    | 定義した追加の認証ガードに割り当てることができます。
    |
    | サポート: "database", "eloquent"
    |
    */

    'providers' => [
        'users' => [
            'driver' => 'eloquent',
            'model' => App\Models\User::class,
        ],

        'admins' => [
            'driver' => 'eloquent',
            'model' => App\Models\Admin::class,
        ],

        // 'users' => [
        //     'driver' => 'database',
        //     'table' => 'users',
        // ],
    ],

    /*
    |--------------------------------------------------------------------------
    | パスワードリセット
    |--------------------------------------------------------------------------
    |
    | アプリケーションに複数のユーザーテーブルやモデルがある場合、
    | 複数のパスワードリセット設定を指定できます。
    | ユーザータイプごとに個別の設定を持つことができます。
    |
    | 有効期限はリセットトークンが有効とみなされる分数です。
    | このセキュリティ機能により、トークンの有効期間を短くし、
    | 推測される可能性を低減します。必要に応じて変更できます。
    |
    | スロットル設定は、ユーザーが次のパスワードリセットトークンを
    | 生成するまでに待つ必要がある秒数です。
    | 大量のトークンが短時間で生成されるのを防ぎます。
    |
    */

    'passwords' => [
        'users' => [
            'provider' => 'users',
            'table' => 'password_reset_tokens',
            'expire' => 60,
            'throttle' => 60,
        ],

        'admins' => [
            'provider' => 'admins',
            'table' => 'password_reset_tokens',
            'expire' => 60,
            'throttle' => 60,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | パスワード確認タイムアウト
    |--------------------------------------------------------------------------
    |
    | パスワード確認がタイムアウトするまでの秒数を定義します。
    | タイムアウト後、ユーザーは確認画面でパスワードの再入力を
    | 求められます。デフォルトでは3時間に設定されています。
    |
    */

    'password_timeout' => 10800,

];